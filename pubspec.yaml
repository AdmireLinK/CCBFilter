name: ccb_filter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  window_manager: ^0.4.2
  lpinyin: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - public/icons/favicon.png
  fonts:
    - family: HarmonyOS_Sans_SC
      fonts:
        - asset: public/fonts/HarmonyOS_Sans_SC.ttf